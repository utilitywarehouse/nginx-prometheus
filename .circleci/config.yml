version: 2
jobs:
  build:
    docker:
      - image: circleci/buildpack-deps:stable
    steps:
      - checkout
      - setup_remote_docker:
          version: 19.03.13
      - run: docker login -u $DOCKER_ID -p $DOCKER_PASSWORD registry.uw.systems
      - run: docker build -t registry.uw.systems/nginx-prometheus:$CIRCLE_SHA1 .
      - run: docker tag registry.uw.systems/nginx-prometheus:$CIRCLE_SHA1 registry.uw.systems/nginx-prometheus:latest
      - run: docker push registry.uw.systems/nginx-prometheus:$CIRCLE_SHA1
      - run: docker push registry.uw.systems/nginx-prometheus:latest

workflows:
  version: 2
  build:
    jobs:
      - build:
          context: cbc-dev
          filters:
            branches:
              only: master

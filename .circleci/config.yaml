version: 2
jobs:
  build:
    docker:
      - image: circleci/buildpack-deps:stable
    steps:
      - checkout
      - setup_remote_docker:
          version: 18.05.0-ce
      - run: docker login -u $DOCKER_ID -p $DOCKER_PASSWORD registry.uw.systems
      - run: docker build -t nginx-prometheus:$CIRCLE_SHA1 .
      - run: docker tag nginx-prometheus:$CIRCLE_SHA1 nginx-prmetheus:latest
      - run: docker push nginx-prometheus:$CIRCLE_SHA1
      - run: docker push nginx-prometheus:latest

workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            branches:
              only: master
